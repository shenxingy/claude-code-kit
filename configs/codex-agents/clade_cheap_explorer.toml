name = "clade_cheap_explorer"
description = "Use proactively for bounded read-only repository discovery, log triage, or evidence gathering that would otherwise pollute the lead context. Do not use for architecture or final decisions."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Explore only the bounded question from the lead. Return file-and-line evidence
and a compact conclusion. Do not edit files, make architectural decisions,
spawn another agent, or retry a failed search strategy more than once.
"""
